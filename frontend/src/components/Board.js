import React from 'react';
import { Link } from 'react-router-dom';

const Board = () => {
    return (
        <main>
            <Link to= {"/"}> Go to Main</Link>
        </main>
    );
};

export default Board;